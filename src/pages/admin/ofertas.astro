---
import ProtectedLayout from '../../layouts/ProtectedLayout.astro';
---

<ProtectedLayout requireAdmin={true}>
  <div class="admin-page">
    <div class="admin-container">
      <!-- Header -->
      <div class="admin-header">
        <a href="/admin/dashboard" class="admin-back-btn">←</a>
        <h1 class="admin-title">Gestión de Ofertas</h1>
        <p class="admin-subtitle">Crea y administra ofertas especiales para tus productos</p>
      </div>

      <!-- Panel de Bienvenida -->
      <div class="ofertas-welcome-panel">
        <div class="ofertas-welcome-content">
          <h2>Impulsa tus Ventas</h2>
          <p>Crea ofertas exclusivas y promociones especiales para atraer a más clientes. Gestiona descuentos, fechas y visibilidad desde un único panel intuitivo.</p>
          <div class="ofertas-stats">
            <div class="ofertas-stat-card">
              <p class="ofertas-stat-label">Ofertas Activas</p>
              <p class="ofertas-stat-value" id="ofertasActivas">--</p>
            </div>
            <div class="ofertas-stat-card">
              <p class="ofertas-stat-label">Total de Ofertas</p>
              <p class="ofertas-stat-value" id="totalOfertas">--</p>
            </div>
          </div>
        </div>
        <div class="ofertas-welcome-action">
          <button id="btnNuevaOferta" class="ofertas-btn-nueva">
            <span>+</span> Nueva Oferta
          </button>
          <p class="ofertas-action-hint">Crea una promoción en segundos</p>
        </div>
      </div>

      <!-- Tabla de Ofertas -->
      <div class="admin-table-container">
        <div id="ofertasContainer" class="admin-loading">
          <p>Cargando ofertas...</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal para crear/editar oferta -->
  <div id="modalOferta" class="admin-modal-overlay">
    <div class="admin-modal">
      <h2 class="admin-modal-title">Nueva Oferta</h2>
      <p class="admin-subtitle">Crea una promoción especial para tus productos premium</p>
      
      <form id="formOferta" class="admin-form">
        
        <!-- Producto -->
        <div class="admin-form-group">
          <label class="admin-label">Selecciona Producto</label>
          <select id="productoSelect" required class="admin-select"></select>
        </div>

        <!-- Nombre Oferta -->
        <div class="admin-form-group">
          <label class="admin-label">Nombre de la Oferta</label>
          <input type="text" id="nombreOferta" required placeholder="Ej: Black Friday 50% Off" class="admin-input" />
        </div>

        <!-- Descripción -->
        <div class="admin-form-group">
          <label class="admin-label">Descripción (opcional)</label>
          <textarea id="descripcion" placeholder="Describe los detalles de la oferta..." class="admin-input ofertas-textarea"></textarea>
        </div>

        <!-- Precios -->
        <div class="admin-form-row ofertas-precios-row">
          <div class="admin-form-group">
            <label class="admin-label">Precio Original (€)</label>
            <input type="number" id="precioOriginal" required step="0.01" placeholder="0.00" readonly class="admin-input ofertas-input-readonly" />
            <p class="ofertas-hint">Auto-llenado desde el producto</p>
          </div>

          <div class="admin-form-group">
            <label class="admin-label">Precio Descuento (€)</label>
            <input type="number" id="precioDescuento" required step="0.01" placeholder="0.00" class="admin-input" />
            <p class="ofertas-hint">Debe ser menor al original</p>
          </div>
        </div>

        <!-- Fechas -->
        <div class="admin-form-row">
          <div class="admin-form-group">
            <label class="admin-label">Fecha Inicio</label>
            <input type="datetime-local" id="fechaInicio" required class="admin-input" />
          </div>

          <div class="admin-form-group">
            <label class="admin-label">Fecha Fin</label>
            <input type="datetime-local" id="fechaFin" required class="admin-input" />
          </div>
        </div>

        <!-- Botones -->
        <div class="admin-form-actions">
          <button type="button" id="btnCancelar" class="admin-btn-secondary">
            Cancelar
          </button>
          <button type="submit" class="admin-btn-primary">
            Guardar Oferta
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Modal de confirmación para eliminar oferta -->
  <div id="modalEliminarOferta" class="admin-modal-overlay" style="display: none;">
    <div class="admin-modal" style="max-width: 400px;">
      <h2 class="admin-modal-title">Confirmar Eliminación</h2>
      <p style="color: var(--text-muted); margin: 1.5rem 0;">
        ¿Está seguro de que desea eliminar esta oferta? Esta acción no se puede deshacer.
      </p>
      <div class="admin-form-actions" style="gap: 1rem;">
        <button id="btnConfirmarEliminarOferta" class="admin-btn-primary" style="background: #ef4444;">
          Eliminar
        </button>
        <button id="btnCancelarEliminarOferta" class="admin-btn-secondary">
          Cancelar
        </button>
      </div>
    </div>
  </div>

  <!-- PopUp de notificaciones -->
  <div id="popupNotificacion" class="popup-notificacion" style="display: none;">
    <div class="popup-icono" id="popupIcono"></div>
    <span class="popup-mensaje" id="popupMensaje"></span>
  </div>

  <script>
    let ofertas: any[] = [];
    let productos: any[] = [];
    let eliminandoOfertaId: number | null = null;
    let popupTimeout: ReturnType<typeof setTimeout> | null = null;

    function showPopup(mensaje: string, tipo: 'success' | 'error' = 'success') {
      const popup = document.getElementById('popupNotificacion')!;
      const icono = document.getElementById('popupIcono')!;
      const msg = document.getElementById('popupMensaje')!;

      if (popupTimeout) clearTimeout(popupTimeout);

      icono.textContent = tipo === 'success' ? '✓' : '✕';
      msg.textContent = mensaje;
      popup.className = `popup-notificacion popup-${tipo} popup-visible`;
      popup.style.display = 'flex';

      popupTimeout = setTimeout(() => {
        popup.classList.remove('popup-visible');
        popup.classList.add('popup-hide');
        setTimeout(() => { popup.style.display = 'none'; popup.classList.remove('popup-hide'); }, 400);
      }, 3000);
    }

    // Cargar productos y ofertas
    async function cargarDatos() {
      try {
        // Cargar productos
        const respProductos = await fetch('/api/admin/productos');
        const dataProductos = await respProductos.json();
        if (dataProductos.success) {
          productos = dataProductos.productos || [];
          actualizarSelectProductos();
        }

        // Cargar ofertas
        cargarOfertas();
      } catch (error) {
        console.error('Error cargando datos:', error);
      }
    }

    function actualizarSelectProductos() {
      const select = document.getElementById('productoSelect') as HTMLSelectElement;
      select.innerHTML = '<option value="">Selecciona un producto...</option>';
      productos.forEach(p => {
        const option = document.createElement('option');
        option.value = p.id;
        option.textContent = p.nombre;
        option.dataset.precio = p.precio_centimos; // Guardar precio en atributo de datos
        select.appendChild(option);
      });
      
      // Agregar evento para llenar el precio original
      select.addEventListener('change', (e) => {
        const selectedOption = (e.target as HTMLSelectElement).selectedOptions[0];
        const precioCentimos = selectedOption?.dataset.precio;
        if (precioCentimos) {
          const precioEnEuros = parseInt(precioCentimos) / 100;
          (document.getElementById('precioOriginal') as HTMLInputElement).value = precioEnEuros.toString();
        } else {
          (document.getElementById('precioOriginal') as HTMLInputElement).value = '';
        }
      });
    }

    async function cargarOfertas() {
      try {
        const response = await fetch('/api/admin/ofertas');
        const data = await response.json();
        if (data.success) {
          ofertas = data.data;
          renderizarOfertas();
          
          // Actualizar estadísticas del panel
          const totalOfertas = ofertas.length;
          const ofertasActivas = ofertas.filter((o: any) => o.activa).length;
          
          document.getElementById('totalOfertas')!.textContent = totalOfertas.toString();
          document.getElementById('ofertasActivas')!.textContent = ofertasActivas.toString();
        }
      } catch (error) {
        console.error('Error cargando ofertas:', error);
      }
    }

    function renderizarOfertas() {
      const container = document.getElementById('ofertasContainer') as HTMLDivElement;
      container.className = '';
      
      if (ofertas.length === 0) {
        container.innerHTML = '<div class="admin-empty-state"><p>No hay ofertas creadas aún</p></div>';
        return;
      }

      let html = '<div class="admin-table-scroll"><table class="admin-table">';
      html += '<thead><tr>';
      html += '<th>Nombre</th>';
      html += '<th>Producto</th>';
      html += '<th>Descuento</th>';
      html += '<th>Fecha Fin</th>';
      html += '<th class="center">Estado</th>';
      html += '<th class="center">Acciones</th>';
      html += '</tr></thead>';
      html += '<tbody>';

      ofertas.forEach(oferta => {
        const descuento = oferta.porcentaje_descuento || 0;
        const estado = oferta.activa ? 'Activa' : 'Inactiva';
        const fechaFin = new Date(oferta.fecha_fin).toLocaleDateString('es-ES');
        
        const toggleStyle = oferta.activa 
          ? 'background:#10b981;color:white;border:2px solid #059669;box-shadow:0 2px 8px rgba(16,185,129,0.4);'
          : 'background:#ef4444;color:white;border:2px solid #dc2626;box-shadow:0 2px 8px rgba(239,68,68,0.4);';
        
        html += `<tr>
          <td>${oferta.nombre_oferta}</td>
          <td>${oferta.producto?.nombre || 'Sin producto'}</td>
          <td><strong class="ofertas-descuento">${descuento}%</strong></td>
          <td>${fechaFin}</td>
          <td class="center">
            <button onclick="toggleActivarOferta(${oferta.id}, ${!oferta.activa})" 
              style="${toggleStyle}padding:0.55rem 1.2rem;border-radius:8px;cursor:pointer;font-weight:700;font-size:0.9rem;transition:all 0.2s;white-space:nowrap;"
              title="${oferta.activa ? 'Desactivar' : 'Activar'}"
              onmouseover="this.style.opacity='0.85';this.style.transform='translateY(-1px)'"
              onmouseout="this.style.opacity='1';this.style.transform='translateY(0)'">
              ${estado}
            </button>
          </td>
          <td class="center">
            <button onclick="editarOferta(${oferta.id})" class="admin-btn-secondary ofertas-btn-accion">Editar</button>
            <button onclick="eliminarOferta(${oferta.id})" class="admin-btn-danger ofertas-btn-accion">Eliminar</button>
          </td>
        </tr>`;
      });

      html += '</tbody></table></div>';
      container.innerHTML = html;
    }

    function abrirModal() {
      const modal = document.getElementById('modalOferta') as HTMLDivElement;
      modal.style.display = 'flex';
      const form = document.getElementById('formOferta') as HTMLFormElement | null;
      if (form) form.reset();
    }

    function cerrarModal() {
      const modal = document.getElementById('modalOferta') as HTMLDivElement;
      modal.style.display = 'none';
      
      // Limpiar el formulario y restaurar valores por defecto
      (document.getElementById('formOferta') as HTMLFormElement).reset();
      delete (document.getElementById('formOferta') as HTMLFormElement).dataset.ofertaId;
      
      const tituloModal = document.querySelector('#modalOferta h2') as HTMLElement;
      const btnEnviar = document.querySelector('#formOferta button[type="submit"]') as HTMLButtonElement;
      tituloModal.textContent = 'Nueva Oferta';
      btnEnviar.textContent = 'Crear Oferta';
    }

    async function guardarOferta(e: any) {
      e.preventDefault();
      
      const productoId = (document.getElementById('productoSelect') as HTMLSelectElement).value;
      const nombreOferta = (document.getElementById('nombreOferta') as HTMLInputElement).value;
      const descripcion = (document.getElementById('descripcion') as HTMLTextAreaElement).value;
      const precioOriginal = parseFloat((document.getElementById('precioOriginal') as HTMLInputElement).value) * 100;
      const precioDescuento = parseFloat((document.getElementById('precioDescuento') as HTMLInputElement).value) * 100;
      const fechaInicio = (document.getElementById('fechaInicio') as HTMLInputElement).value;
      const fechaFin = (document.getElementById('fechaFin') as HTMLInputElement).value;
      
      const ofertaId = (document.getElementById('formOferta') as HTMLFormElement).dataset.ofertaId;
      const isEdicion = !!ofertaId;

      try {
        const method = isEdicion ? 'PUT' : 'POST';
        const url = isEdicion ? `/api/admin/ofertas/${ofertaId}` : '/api/admin/ofertas';

        const response = await fetch(url, {
          method: method,
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({
            producto_id: parseInt(productoId),
            nombre_oferta: nombreOferta,
            descripcion: descripcion || null,
            precio_original_centimos: Math.round(precioOriginal),
            precio_descuento_centimos: Math.round(precioDescuento),
            fecha_inicio: new Date(fechaInicio).toISOString(),
            fecha_fin: new Date(fechaFin).toISOString()
          })
        });

        const data = await response.json();
        if (data.success) {
          cerrarModal();
          cargarOfertas();
          showPopup(isEdicion ? 'Oferta actualizada exitosamente' : 'Oferta creada exitosamente', 'success');
          
          // Limpiar el dataset después de editar
          if (isEdicion) {
            delete (document.getElementById('formOferta') as HTMLFormElement).dataset.ofertaId;
            const tituloModal = document.querySelector('#modalOferta h2') as HTMLElement;
            const btnEnviar = document.querySelector('#formOferta button[type="submit"]') as HTMLButtonElement;
            tituloModal.textContent = 'Nueva Oferta';
            btnEnviar.textContent = 'Crear Oferta';
          }
        } else {
          showPopup('Error: ' + data.error, 'error');
        }
      } catch (error) {
        console.error('Error guardando oferta:', error);
        showPopup('Error al guardar la oferta', 'error');
      }
    }

    // Hacer funciones globales para onclick
    (window as any).abrirModal = abrirModal;
    (window as any).cerrarModal = cerrarModal;
    (window as any).guardarOferta = guardarOferta;
    (window as any).editarOferta = editarOferta;
    (window as any).eliminarOferta = eliminarOferta;
    (window as any).toggleActivarOferta = toggleActivarOferta;

    // Event Listeners para modal de eliminar oferta
    document.getElementById('btnConfirmarEliminarOferta')!.addEventListener('click', async () => {
      if (eliminandoOfertaId) {
        try {
          const response = await fetch(`/api/admin/ofertas/${eliminandoOfertaId}`, { 
            method: 'DELETE',
            credentials: 'include'
          });
          const data = await response.json();
          if (data.success) {
            cargarOfertas();
            document.getElementById('modalEliminarOferta')!.style.display = 'none';
            eliminandoOfertaId = null;
            showPopup('Oferta eliminada exitosamente', 'success');
          } else {
            showPopup('Error: ' + data.error, 'error');
          }
        } catch (error) {
          console.error('Error eliminando oferta:', error);
          showPopup('Error al eliminar la oferta', 'error');
        }
      }
    });

    document.getElementById('btnCancelarEliminarOferta')!.addEventListener('click', () => {
      document.getElementById('modalEliminarOferta')!.style.display = 'none';
      eliminandoOfertaId = null;
    });

    document.getElementById('modalEliminarOferta')!.addEventListener('click', (e) => {
      if ((e.target as HTMLElement).id === 'modalEliminarOferta') {
        document.getElementById('modalEliminarOferta')!.style.display = 'none';
        eliminandoOfertaId = null;
      }
    });

    async function editarOferta(id: number) {
      const oferta = ofertas.find(o => o.id === id);
      if (!oferta) {
        showPopup('Oferta no encontrada', 'error');
        return;
      }

      // Abrir el modal primero
      abrirModal();

      // Esperar un poco para que el DOM se actualice
      await new Promise(resolve => setTimeout(resolve, 100));

      // Poblar el formulario con los datos de la oferta
      const selectProducto = document.getElementById('productoSelect') as HTMLSelectElement;
      const inputNombre = document.getElementById('nombreOferta') as HTMLInputElement;
      const inputDescripcion = document.getElementById('descripcion') as HTMLTextAreaElement;
      const inputPrecioOriginal = document.getElementById('precioOriginal') as HTMLInputElement;
      const inputPrecioDescuento = document.getElementById('precioDescuento') as HTMLInputElement;
      const inputFechaInicio = document.getElementById('fechaInicio') as HTMLInputElement;
      const inputFechaFin = document.getElementById('fechaFin') as HTMLInputElement;

      // Asignar valores - producto_id viene como oferta.producto.id por el join
      const productoId = oferta.producto?.id || oferta.producto_id;
      if (productoId) selectProducto.value = productoId.toString();
      inputNombre.value = oferta.nombre_oferta;
      inputDescripcion.value = oferta.descripcion || '';
      inputPrecioOriginal.value = (oferta.precio_original_centimos / 100).toString();
      inputPrecioDescuento.value = (oferta.precio_descuento_centimos / 100).toString();
      
      // Formato correcto para datetime-local
      const fechaInicioISO = new Date(oferta.fecha_inicio).toISOString().slice(0, 16);
      const fechaFinISO = new Date(oferta.fecha_fin).toISOString().slice(0, 16);
      
      inputFechaInicio.value = fechaInicioISO;
      inputFechaFin.value = fechaFinISO;

      // Cambiar el título del modal
      const tituloModal = document.querySelector('#modalOferta h2') as HTMLElement;
      const btnEnviar = document.querySelector('#formOferta button[type="submit"]') as HTMLButtonElement;
      
      tituloModal.textContent = 'Editar Oferta';
      btnEnviar.textContent = 'Actualizar Oferta';

      // Guardar el ID de la oferta para saber que es una edición
      (document.getElementById('formOferta') as HTMLFormElement).dataset.ofertaId = id.toString();
    }

    async function eliminarOferta(id: number) {
      eliminandoOfertaId = id;
      document.getElementById('modalEliminarOferta')!.style.display = 'flex';
    }

    async function toggleActivarOferta(id: number, activar: boolean) {
      try {
        const response = await fetch(`/api/admin/ofertas/${id}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ activa: activar })
        });

        const data = await response.json();
        if (data.success) {
          const oferta = ofertas.find(o => o.id === id);
          if (oferta) {
            oferta.activa = activar;
          }
          const status = activar ? 'activada' : 'desactivada';
          cargarOfertas();
          showPopup(`Oferta ${status} exitosamente`, 'success');
        } else {
          showPopup('Error: ' + data.error, 'error');
        }
      } catch (error) {
        console.error('Error toggling oferta:', error);
        showPopup('Error al actualizar el estado de la oferta', 'error');
      }
    }

    // Event listeners
    document.getElementById('btnNuevaOferta')?.addEventListener('click', abrirModal);
    document.getElementById('btnCancelar')?.addEventListener('click', cerrarModal);
    document.getElementById('formOferta')?.addEventListener('submit', guardarOferta);

    // Cargar datos al iniciar
    cargarDatos();
  </script>

  <style>
    /* Panel de bienvenida */
    .ofertas-welcome-panel {
      background: linear-gradient(135deg, #a89968 0%, #8b7355 100%);
      border-radius: 16px;
      padding: 3rem;
      margin-bottom: 3rem;
      box-shadow: 0 15px 40px rgba(168, 153, 104, 0.25);
      border: 1px solid rgba(255, 255, 255, 0.1);
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 3rem;
      align-items: center;
    }

    .ofertas-welcome-content h2 {
      color: white;
      font-size: 2rem;
      margin: 0 0 1rem 0;
      font-weight: 900;
    }

    .ofertas-welcome-content > p {
      color: rgba(255, 255, 255, 0.9);
      font-size: 1.1rem;
      margin: 0 0 1.5rem 0;
      line-height: 1.6;
    }

    .ofertas-stats {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .ofertas-stat-card {
      background: rgba(255, 255, 255, 0.15);
      padding: 1rem;
      border-radius: 10px;
      flex: 1;
      min-width: 150px;
      text-align: center;
    }

    .ofertas-stat-label {
      color: rgba(255, 255, 255, 0.8);
      font-size: 0.9rem;
      margin: 0;
    }

    .ofertas-stat-value {
      color: white;
      font-size: 1.8rem;
      margin: 0.5rem 0 0 0;
      font-weight: 900;
    }

    .ofertas-welcome-action {
      text-align: center;
    }

    .ofertas-btn-nueva {
      background: white;
      color: #8b7355;
      padding: 1.5rem 2.5rem;
      border: none;
      border-radius: 12px;
      font-size: 1.1rem;
      font-weight: 800;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      display: flex;
      align-items: center;
      gap: 0.75rem;
      white-space: nowrap;
    }

    .ofertas-btn-nueva:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
    }

    .ofertas-btn-nueva span {
      font-size: 1.5rem;
    }

    .ofertas-action-hint {
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.9rem;
      margin-top: 1rem;
    }

    /* Formulario */
    .ofertas-textarea {
      min-height: 80px;
      resize: vertical;
      font-family: inherit;
    }

    .ofertas-precios-row {
      background: linear-gradient(135deg, rgba(168, 153, 104, 0.05) 0%, rgba(139, 115, 85, 0.05) 100%);
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid rgba(168, 153, 104, 0.1);
    }

    .ofertas-input-readonly {
      background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%) !important;
      cursor: not-allowed !important;
      color: #64748b !important;
      border-color: rgba(168, 153, 104, 0.3) !important;
    }

    .ofertas-hint {
      margin: 0.5rem 0 0 0;
      color: #94a3b8;
      font-size: 0.85rem;
    }

    /* Tabla */
    .ofertas-descuento {
      color: #dc2626;
    }

    .ofertas-btn-accion {
      padding: 0.35rem 0.75rem;
      font-size: 0.85rem;
    }

    /* PopUp notificaciones */
    .popup-notificacion {
      position: fixed;
      top: 2rem;
      right: 2rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem 1.5rem;
      border-radius: 12px;
      font-size: 0.95rem;
      font-weight: 600;
      z-index: 10000;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      opacity: 0;
      transform: translateX(100%);
      transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .popup-visible {
      opacity: 1;
      transform: translateX(0);
    }

    .popup-hide {
      opacity: 0;
      transform: translateX(100%);
    }

    .popup-success {
      background: linear-gradient(135deg, #065f46 0%, #047857 100%);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.15);
    }

    .popup-error {
      background: linear-gradient(135deg, #991b1b 0%, #dc2626 100%);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.15);
    }

    .popup-icono {
      width: 28px;
      height: 28px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
      font-weight: 900;
      flex-shrink: 0;
    }

    .popup-success .popup-icono {
      background: rgba(255, 255, 255, 0.2);
    }

    .popup-error .popup-icono {
      background: rgba(255, 255, 255, 0.2);
    }

    .popup-mensaje {
      max-width: 300px;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .ofertas-welcome-panel {
        grid-template-columns: 1fr;
        gap: 2rem;
        padding: 2rem;
      }

      .ofertas-welcome-action {
        text-align: left;
      }

      .ofertas-btn-nueva {
        width: 100%;
        justify-content: center;
      }
    }

    .ofertas-toggle-btn:hover {
      transform: scale(1.1);
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }

    .ofertas-toggle-btn:active {
      transform: scale(0.95);
    }

    .ofertas-toggle-switch {
      padding: 0.7rem 1.5rem !important;
      border: 2px solid !important;
      border-radius: 8px !important;
      cursor: pointer !important;
      font-weight: 700 !important;
      font-size: 0.95rem !important;
      transition: all 0.3s ease !important;
      white-space: nowrap !important;
      display: inline-block !important;
      text-transform: capitalize !important;
      letter-spacing: 0.5px !important;
      outline: none !important;
    }

    .ofertas-toggle-switch.activo {
      background: #10b981 !important;
      color: white !important;
      border-color: #059669 !important;
      box-shadow: 0 2px 8px rgba(16, 185, 129, 0.4) !important;
    }

    .ofertas-toggle-switch.activo:hover {
      background: #059669 !important;
      border-color: #047857 !important;
      box-shadow: 0 4px 14px rgba(16, 185, 129, 0.6) !important;
      transform: translateY(-2px) !important;
    }

    .ofertas-toggle-switch.activo:active {
      transform: translateY(0) !important;
    }

    .ofertas-toggle-switch.inactivo {
      background: #ef4444 !important;
      color: white !important;
      border-color: #dc2626 !important;
      box-shadow: 0 2px 8px rgba(239, 68, 68, 0.4) !important;
    }

    .ofertas-toggle-switch.inactivo:hover {
      background: #dc2626 !important;
      border-color: #b91c1c !important;
      box-shadow: 0 4px 14px rgba(239, 68, 68, 0.6) !important;
      transform: translateY(-2px) !important;
    }

    .ofertas-toggle-switch.inactivo:active {
      transform: translateY(0) !important;
    }
  </style>
</ProtectedLayout>
